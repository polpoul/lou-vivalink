server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html/lou;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /manage {
        try_files /manage.html =404;
    }
}
